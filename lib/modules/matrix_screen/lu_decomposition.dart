import 'package:flutter/material.dart';

class LuDecomposition extends StatelessWidget {
  const LuDecomposition({Key? key}) : super(key: key);



  @override
  Widget build(BuildContext context) {
    return Center(child: Text('lu'),);
  }
}
