import 'package:flutter/material.dart';

class FixedPointScreen extends StatelessWidget {
  const FixedPointScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('FixedPoint'),
      ),
    );
  }
}
